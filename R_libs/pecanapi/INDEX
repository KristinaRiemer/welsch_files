add_database            Add PEcAn database information to settings list
add_pft                 Add a PFT or list of PFTs to a settings object
add_rabbitmq            Add RabbitMQ configuration
add_workflow            Add information from workflow data frame into
                        settings list.
dbfile_url              Build a THREDDS fileServer URL for a specific
                        dbfile
get_model_id            Retrieve database ID of a particular version of
                        a model
get_next_workflow_id    Get current workflow ID and update internal
                        workflow ID PostgreSQL sequence
get_run_id              Get a run ID from the 'runs.txt' file
insert_new_workflow     Insert a new workflow into PEcAn database,
                        returning the workflow as a 'data.frame'
listToXML               Convert List to XML
list_runs               List all runs associated with a particular
                        workflow
output_url              Build URL for output file hosted on THREDDS
                        fileServer
pecanapi_options        Package-specific options
prepared_query          Execute a PostgreSQL prepared query or
                        statement
run_dap                 Create a THREDDS OpenDAP access URL to a
                        specific model output file
run_url                 Build a THREDDS fileServer URL for a specific
                        output file from a specific run
search_models           Search for sites or models
submit_workflow         Post complete settings list as RabbitMQ message
thredds_dap_url         Create a THREDDS OpenDAP access URL to a PEcAn
                        file
thredds_fs_url          Create a THREDDS fileServer URL prefix
watch_workflow          Continuously monitor the progress of a workflow
                        until it completes. To exit early, send an
                        interrupt signal (Control-C).
workflow_output         Retrieve the current output of a workflow given
                        its ID
